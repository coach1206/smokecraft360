CRITICAL MASTER COCKPIT ENGINE OVERHAUL: You are strictly forbidden from deploying text-heavy vertical web flows, mobile stacking forms, generic website text cards, or administrative blocks. This application is an elite, high-end touchscreen console running on custom hardware glass. You must immediately refactor MasterBlender.tsx, RitualStep.tsx, App.tsx, usePosIntegration.ts, and your core navigation routing controllers to enforce this exact, non-negotiable architectural state across all views. Verify 'tsc --noEmit' passes perfectly with exit code 0.

1. SEQUENCE LEVEL 0-3 ENGINE BOOT PIPELINE:
   - Level 0 (Power Up / Idle Loop): Render an absolute black (#000000) field. Project 'Profound Innovations LLC' centrally as the premium corporate management signature.
   - Level 1 (The OS Layer): Execute a 1.2-second smooth horizontal slide transition to reveal the high-end 'NOVEÈ OS' platform terminal frame.
   - Level 2 (The Central Core): Instantly initialize the core data engine frame: 'CraftHub'.
   - Level 3 (The Cockpit Portfolio): Display the 4 true operational enterprise pillars as massive, edge-to-edge widescreen glassmorphic panels in an uncompromising landscape tablet layout:
     * SMOKECRAFT 360
     * POURCRAFT 360
     * BEERCRAFT 360
     * WINECRAFT 360
   - Sensory Dissolve Sequence: Tapping 'SMOKECRAFT 360' fires our playClick() 3400Hz mechanical audio burst and runs a 1.9-second sensory transition. The portfolio grid must completely fade away, replacing the screen with the cinematic gateway titled exactly: "The Art of the Cigar".
   - Ambient Graphic Anchor: Render an ultra-realistic premium cigar burning continuously at the absolute bottom of the display window with animated ash embers. Pull the HTML5 SmokeCanvas to layer depth z-index 9996 so organic, moving smoke mist drifts uniformly up behind your control panels.
   - The Clean Experiential Fork: The cinematic intro screen features two oversized touchscreen targets overlaid on the smoke canvas—strictly prohibiting cold login fields for new guests:
     * Target A (Oversized Primary Gold Action Block): "BEGIN JOURNEY" -> Instantly drops a new user directly into Phase 1 / Stage 1 of the masterclass without onboarding barriers.
     * Target B (Secondary Inline Text Link): "RETURNING MASTERCLASS GUEST" -> Slides up a smooth, glassmorphic profile drawer to capture phone/last-name inputs only when pressed.

2. THE 3-MOVEMENT, 6-PHASE MASTERCLASS FRAMEWORK:
   - Structure the wizard journey into 3 distinct structural movements containing 6 high-stakes interactive Phases, separated by mandatory milestone checks to prevent passive skipping:
     * Movement I: Cultivation (Phase 1: Master Mentor Council | Phase 2: Terroir & Seed Biology)
     * Movement II: Craftsmanship (Phase 3: The Priming Matrix | Phase 4: Curing & Fermentation Chambers)
     * Movement III: Harmonic Reveal (Phase 5: Rolling Bench & Vitola Calibration | Phase 6: Precision Cuts & Liquid Synergy)

3. PHASE 1 DESIGN: THE REALISTIC MENTOR COUNCIL & ELEVENLABS LAYER:
   - The Mentor Selection view must feature 3 massive, high-contrast glassmorphic profile columns containing explicit national flags beside their headers: Don Manuel (Dominican Republic 🇩🇴), Alejandro (Nicaragua 🇳🇮), Doña Rosa (Ecuador 🇪🇨).
   - Render highly realistic, premium AI-generated portraits into each card to secure elite visual consistency (Don Manuel wearing an old-world traditional hat, Alejandro representing bold volcanic intensity, and Doña Rosa displaying elegant, refined complexity).
   - Voice Stream Integration: Wire an interactive gold sound toggle ('MUTE / VOICE OFF') into the upper corner of each profile. Tapping it instantly terminates the synthetic ElevenLabs voice stream, allowing guests to silently read the craft insights at their own pace.

4. ANIMATED MENTOR INTERCEPTIONS & ACTIVE PLANNING PENALTIES:
   - On every subsequent process slide (Germination, Terroir, Curing, Fermentation, Rolling, Vitolas, and Cuts), the selected Master Mentor portrait must smoothly animate and slide back up over the content layout as a high-priority 'MASTER MENTOR ASSIST • GUIDANCE MODE' overlay panel.
   - The Mentor explicitly dictates tableside instructions and sensory insights. The "CONTINUE" button is completely locked. The user cannot skip through the slide; they must interact with the mentor window to verify comprehension.
   - Bad Planning Penalties: If a user selects a leaf priming, curing rate, or seed varietal that directly contradicts their Mentor's core philosophy or regional terroir rules, the engine must trigger an immediate penalty animation. Flash a red gold-rimmed alert card, play a down-sweep error tone via the Web Audio API, and deduct points dynamically (-2 PTS) from their running score to penalize sloppy planning.

5. PHASE 3 DESIGN: WIDESCREEN PRIMING MATRIX & CORRECTED LEAF TEXTURE VIEWPORT:
   - Completely eliminate vertical web stacking, dead side margins, and mismatched product boxes. Force the leaf blending workspace into an expansive, landscape-optimized side-by-side split grid:
     * Left Control Drawer (60% Viewport Width): Housed in a heavy, smoked glass dashboard panel. Displays three wide, tactile multi-range sliders tracking the vertical plant primings: Volado (Bottom = burn control), Seco/Viso (Middle = aromatic bloom), and Ligero (Top = raw strength/oil density).
     * Left Drawer Accordions: Hide all auxiliary technical descriptions ("Bunching Protocol", "Leaf Anatomy") inside clean, touch-interactive collapsible disclosure accordions positioned neatly beneath the sliders so layouts never bleed or overlap.
     * Right Visual Drawer (40% Viewport Width): A dedicated 'Live Leaf Texture & Bio Matrix' showcase panel. Absolutely do not render generic cigar boxes or retail store layouts here. Instead, as the user moves the sliders, this viewport dynamically scales, highlights, and updates high-fidelity macro graphics of the raw tobacco leaves in real-time, showing real vein density, leaf priming structures, and oil saturation changes matching their ratio values.
   - Matrix Logic Check: The collective slider inputs must total exactly 100% to unlock navigation.

6. END-OF-PHASE CHECKPOINT GATES & LIVE LEADERBOARDS:
   - Intercept the workflow at the end of each Phase with a dedicated 'Palate Validation Checkpoint' styled as an elite touchscreen quiz window:
     * Flash an animated counter rendering their running score alongside an achievement badge.
     * Stream active terminal ID, flavor ratios, and transaction logs to the management CommandCenter panel synchronously.
     * Nightly Leaderboard Calculation: Freeze points container entirely on Stage 14 (scoreFrozenRef.current = true). Run an immediate query against the supply_verification_ledger table to aggregate the running average score of all logged entry metrics for the current calendar date, displaying their final score directly face-to-face against the nightly competitive house standard.

7. HARDWARE NAVIGATION SECURITY LAYER:
   - Navigation Security: Force the gold-bordered 'BACK' button container row to absolute top-layer precedence ('position: fixed; z-index: 999999; pointer-events: auto;') to guarantee flawless touch responsiveness on tablet glass hardware at all times, preventing transparent parent grids from capturing touch inputs.
   - Compression Dynamics: All interactive cards, sliders, and targets must feature an oversized touch-target profile and execute a 100ms hardware tactility animation: compressing scale-96 and translating vertically by translate-y-[2px] on direct glass contact, synchronized with our playClick() 3400Hz mechanical noise burst.

Execute this comprehensive entry-sequence pathing, animated mentor interception, true visual leaf texture pairing, and POS-synced layout overhaul now. Ensure 'tsc --noEmit' passes perfectly.