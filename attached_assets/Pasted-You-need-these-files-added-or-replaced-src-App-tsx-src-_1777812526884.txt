You need these files added or replaced:
src/App.tsx
src/components/BootIntro.tsx
src/pages/EntryScreen.tsx
src/pages/SmokeCraftDashboard.tsx
src/pages/PairingEngine.tsx
src/pages/SessionMode.tsx
src/styles/smokecraft-system.css
public/logo/profound.png
public/sounds/arrival.mp3
Impact if missing: boot screen will not run, portal cards will not animate, AI voice will not trigger, dashboard will not load, pairing flow will stop, and session mode will feel unfinished.
FAILURE PREVENTION CHECKLIST
Make sure your logo file is named exactly:
public/logo/profound.png
Make sure your sound file is named exactly:
public/sounds/arrival.mp3
Install Framer Motion:
npm install framer-motion
Now paste these files.
MASTER PROMPT
Build the Profound Innovation boot system and SmokeCraft 360 entry experience as one connected React/Vite interface. Use a matte olive green background, deep navy text, flat logo treatment, cinematic slide-in boot animation, arrival sound, portal-style cards, AI voice guidance, SmokeCraft dashboard, pairing engine, result screen, and session mode. No glyphs, no emojis, no cheap glow, no 3D raised lettering. Everything must feel like a premium software system initializing and guiding the user into SmokeCraft 360.
CODE
src/App.tsx
import { useState } from "react";import BootIntro from "./components/BootIntro";import EntryScreen from "./pages/EntryScreen";import SmokeCraftDashboard from "./pages/SmokeCraftDashboard";import PairingEngine from "./pages/PairingEngine";import SessionMode from "./pages/SessionMode";import "./styles/smokecraft-system.css";export type AppScreen =  | "entry"  | "smokecraft"  | "pairing"  | "session"  | "pourcraft";export default function App() {  const [bootDone, setBootDone] = useState(false);  const [screen, setScreen] = useState<AppScreen>("entry");  const [sessionData, setSessionData] = useState<any>(null);  return (    <>      {!bootDone && <BootIntro onFinish={() => setBootDone(true)} />}      {bootDone && screen === "entry" && (        <EntryScreen onEnter={(next) => setScreen(next)} />      )}      {bootDone && screen === "smokecraft" && (        <SmokeCraftDashboard          onBack={() => setScreen("entry")}          onPairing={() => setScreen("pairing")}        />      )}      {bootDone && screen === "pairing" && (        <PairingEngine          onBack={() => setScreen("smokecraft")}          onStartSession={(data) => {            setSessionData(data);            setScreen("session");          }}        />      )}      {bootDone && screen === "session" && (        <SessionMode          data={sessionData}          onBack={() => setScreen("pairing")}          onExit={() => setScreen("smokecraft")}        />      )}      {bootDone && screen === "pourcraft" && (        <div className="system-shell">          <button className="back-button" onClick={() => setScreen("entry")}>            Back          </button>          <div className="coming-soon-card">            <h1>PourCraft 360</h1>            <p>Spirits and pairing system coming online next.</p>          </div>        </div>      )}    </>  );}
src/components/BootIntro.tsx
import { useEffect, useState } from "react";import { motion, AnimatePresence } from "framer-motion";export default function BootIntro({ onFinish }: { onFinish: () => void }) {  const [fadeOut, setFadeOut] = useState(false);  useEffect(() => {    const alreadyPlayed = sessionStorage.getItem("profoundBootPlayed");    if (alreadyPlayed) {      onFinish();      return;    }    sessionStorage.setItem("profoundBootPlayed", "true");    const audio = new Audio("/sounds/arrival.mp3");    audio.volume = 0.45;    const soundTimer = setTimeout(() => {      audio.play().catch(() => {});    }, 850);    const fadeTimer = setTimeout(() => setFadeOut(true), 3900);    const finishTimer = setTimeout(() => onFinish(), 5000);    return () => {      clearTimeout(soundTimer);      clearTimeout(fadeTimer);      clearTimeout(finishTimer);    };  }, [onFinish]);  const skip = () => {    setFadeOut(true);    setTimeout(onFinish, 350);  };  return (    <AnimatePresence>      {!fadeOut && (        <motion.div          className="boot-screen"          onClick={skip}          initial={{ opacity: 1 }}          exit={{ opacity: 0 }}          transition={{ duration: 1 }}        >          <motion.img            src="/logo/profound.png"            alt="Profound Innovation"            className="boot-logo"            initial={{ x: "-120vw", opacity: 0 }}            animate={{ x: 0, opacity: 1 }}            transition={{ duration: 1.1, ease: "easeOut" }}          />          <motion.div            className="boot-copy"            initial={{ opacity: 0, y: 12 }}            animate={{ opacity: 1, y: 0 }}            transition={{ delay: 1.2, duration: 0.9 }}          >            <h1>PROFOUND</h1>            <h2>INNOVATION</h2>            <p>Software & Systems Development Company</p>            <span>Digital Infrastructure Studio</span>          </motion.div>        </motion.div>      )}    </AnimatePresence>  );}
src/pages/EntryScreen.tsx
import { motion } from "framer-motion";import type { AppScreen } from "../App";type Props = {  onEnter: (screen: AppScreen) => void;};function speak(text: string) {  if (!("speechSynthesis" in window)) return;  window.speechSynthesis.cancel();  const voice = new SpeechSynthesisUtterance(text);  voice.rate = 0.88;  voice.pitch = 0.78;  voice.volume = 0.55;  window.speechSynthesis.speak(voice);}export default function EntryScreen({ onEnter }: Props) {  return (    <main className="entry-screen">      <motion.div        className="entry-title"        initial={{ opacity: 0, y: 18 }}        animate={{ opacity: 1, y: 0 }}        transition={{ duration: 0.9 }}        onAnimationComplete={() => speak("Select your experience.")}      >        <h1>Choose Your Experience</h1>        <p>Profound Innovation Experience Systems</p>      </motion.div>      <section className="portal-grid">        <motion.button          className="portal-card smokecraft-card"          whileHover={{ scale: 1.035 }}          whileTap={{ scale: 1.12 }}          onMouseEnter={() => speak("SmokeCraft. Cigar experience system.")}          onClick={() => {            speak("Entering SmokeCraft.");            setTimeout(() => onEnter("smokecraft"), 550);          }}        >          <div className="portal-overlay" />          <div className="portal-content">            <h2>SmokeCraft 360</h2>            <p>Cigar. Atmosphere. Experience.</p>          </div>        </motion.button>        <motion.button          className="portal-card pourcraft-card"          whileHover={{ scale: 1.035 }}          whileTap={{ scale: 1.12 }}          onMouseEnter={() => speak("PourCraft. Spirits and pairing system.")}          onClick={() => {            speak("Entering PourCraft.");            setTimeout(() => onEnter("pourcraft"), 550);          }}        >          <div className="portal-overlay" />          <div className="portal-content">            <h2>PourCraft 360</h2>            <p>Spirits. Pairing. Precision.</p>          </div>        </motion.button>      </section>    </main>  );}
src/pages/SmokeCraftDashboard.tsx
import { motion } from "framer-motion";type Props = {  onBack: () => void;  onPairing: () => void;};export default function SmokeCraftDashboard({ onBack, onPairing }: Props) {  return (    <main className="dashboard-shell">      <aside className="side-nav">        <h2>SmokeCraft 360</h2>        <button>Dashboard</button>        <button onClick={onPairing}>Pairing Engine</button>        <button>My Experiences</button>        <button>Lounge Mode</button>        <button>Settings</button>        <button className="nav-back" onClick={onBack}>Back</button>      </aside>      <section className="dashboard-main">        <motion.div          className="dashboard-hero"          initial={{ opacity: 0, y: 16 }}          animate={{ opacity: 1, y: 0 }}        >          <h1>What experience are you creating today?</h1>          <p>            Build cigar moments around mood, strength, flavor, drink pairing,            and setting.          </p>        </motion.div>        <div className="action-grid">          <button className="action-card cigar-bg" onClick={onPairing}>            <h3>Find a Cigar Pairing</h3>            <p>Match mood, body, flavor, and drink profile.</p>          </button>          <button className="action-card lounge-bg">            <h3>Build a Session</h3>            <p>Create a guided smoke experience with timer and atmosphere.</p>          </button>          <button className="action-card whiskey-bg">            <h3>Explore Lounge Experiences</h3>            <p>Design premium moments for lounges, guests, and members.</p>          </button>        </div>      </section>    </main>  );}
src/pages/PairingEngine.tsx
import { useState } from "react";import { motion } from "framer-motion";type Props = {  onBack: () => void;  onStartSession: (data: any) => void;};const steps = ["Mood", "Strength", "Flavor", "Drink", "Result"];const recommendations: any = {  Relaxed: {    name: "Velvet Ember Session",    cigar: "Medium-bodied Connecticut or Cameroon wrapper",    drink: "Aged rum, bourbon neat, or espresso",    environment: "Low light lounge, soft music, leather seating",  },  Celebratory: {    name: "Gold Room Celebration",    cigar: "Full-bodied Maduro or Habano",    drink: "Single malt whiskey or champagne cocktail",    environment: "Private table, premium glassware, high-energy setting",  },  Focused: {    name: "Quiet Power Session",    cigar: "Medium-bold Nicaraguan blend",    drink: "Black coffee, rye whiskey, or sparkling water",    environment: "Quiet corner, low distraction, clean air flow",  },  Social: {    name: "Roundtable Smoke",    cigar: "Balanced Dominican blend",    drink: "Old Fashioned, rum punch, or craft lager",    environment: "Group seating, warm lighting, conversation-forward setup",  },};export default function PairingEngine({ onBack, onStartSession }: Props) {  const [step, setStep] = useState(0);  const [answers, setAnswers] = useState<any>({    mood: "",    strength: "",    flavor: "",    drink: "",  });  const progress = ((step + 1) / steps.length) * 100;  const result = recommendations[answers.mood] || recommendations.Relaxed;  const select = (key: string, value: string) => {    setAnswers((prev: any) => ({ ...prev, [key]: value }));    setTimeout(() => setStep((prev) => Math.min(prev + 1, steps.length - 1)), 250);  };  return (    <main className="pairing-shell">      <button className="back-button" onClick={step === 0 ? onBack : () => setStep(step - 1)}>        Back      </button>      <div className="progress-wrap">        <div className="progress-bar" style={{ width: `${progress}%` }} />      </div>      <motion.section        key={step}        className="pairing-card"        initial={{ opacity: 0, x: 26 }}        animate={{ opacity: 1, x: 0 }}        transition={{ duration: 0.45 }}      >        {step === 0 && (          <>            <h1>Select Mood</h1>            <div className="choice-grid">              {["Relaxed", "Celebratory", "Focused", "Social"].map((x) => (                <button onClick={() => select("mood", x)} key={x}>{x}</button>              ))}            </div>          </>        )}        {step === 1 && (          <>            <h1>Select Strength</h1>            <div className="choice-grid">              {["Mild", "Medium", "Bold", "Full"].map((x) => (                <button onClick={() => select("strength", x)} key={x}>{x}</button>              ))}            </div>          </>        )}        {step === 2 && (          <>            <h1>Select Flavor Profile</h1>            <div className="choice-grid">              {["Spicy", "Sweet", "Earthy", "Bold"].map((x) => (                <button onClick={() => select("flavor", x)} key={x}>{x}</button>              ))}            </div>          </>        )}        {step === 3 && (          <>            <h1>Select Drink Pairing</h1>            <div className="choice-grid">              {["Whiskey", "Rum", "Wine", "Coffee", "No Drink"].map((x) => (                <button onClick={() => select("drink", x)} key={x}>{x}</button>              ))}            </div>          </>        )}        {step === 4 && (          <div className="result-screen">            <div className="result-image" />            <h1>{result.name}</h1>            <p>              Built for a {answers.mood.toLowerCase()} mood with a{" "}              {answers.strength.toLowerCase()} strength preference and{" "}              {answers.flavor.toLowerCase()} flavor profile.            </p>            <div className="result-details">              <p><strong>Cigar:</strong> {result.cigar}</p>              <p><strong>Drink:</strong> {answers.drink === "No Drink" ? "No drink selected" : result.drink}</p>              <p><strong>Environment:</strong> {result.environment}</p>            </div>            <button              className="primary-action"              onClick={() => onStartSession({ ...answers, ...result })}            >              Start Session            </button>          </div>        )}      </motion.section>    </main>  );}
src/pages/SessionMode.tsx
import { useEffect, useState } from "react";type Props = {  data: any;  onBack: () => void;  onExit: () => void;};export default function SessionMode({ data, onBack, onExit }: Props) {  const [seconds, setSeconds] = useState(0);  useEffect(() => {    const timer = setInterval(() => setSeconds((s) => s + 1), 1000);    return () => clearInterval(timer);  }, []);  const minutes = Math.floor(seconds / 60);  const remaining = seconds % 60;  return (    <main className="session-mode">      <button className="back-button" onClick={onBack}>Back</button>      <div className="session-panel">        <h1>{data?.name || "SmokeCraft Session"}</h1>        <p>{data?.environment || "Premium guided cigar atmosphere."}</p>        <div className="session-timer">          {String(minutes).padStart(2, "0")}:{String(remaining).padStart(2, "0")}        </div>        <div className="session-copy">          <p><strong>Cigar:</strong> {data?.cigar}</p>          <p><strong>Drink:</strong> {data?.drink}</p>        </div>        <button className="primary-action" onClick={onExit}>          End Session        </button>      </div>    </main>  );}
src/styles/smokecraft-system.css
* {  box-sizing: border-box;}body {  margin: 0;  font-family: Inter, Arial, sans-serif;  background: #c5c8b4;  color: #0b2a4a;}button {  font-family: inherit;}.boot-screen {  width: 100vw;  height: 100vh;  background:    linear-gradient(135deg, rgba(255,255,255,0.15), rgba(0,0,0,0.08)),    #c5c8b4;  display: flex;  flex-direction: column;  align-items: center;  justify-content: center;}.boot-logo {  width: min(360px, 68vw);  height: auto;  object-fit: contain;}.boot-copy {  text-align: center;  margin-top: 24px;  color: #0b2a4a;}.boot-copy h1 {  margin: 0;  font-size: 34px;  letter-spacing: 8px;  font-weight: 700;}.boot-copy h2 {  margin: 8px 0 0;  font-size: 18px;  letter-spacing: 5px;  font-weight: 400;}.boot-copy p {  margin: 14px 0 0;  font-size: 12px;  letter-spacing: 2px;}.boot-copy span {  display: block;  margin-top: 12px;  font-size: 11px;  letter-spacing: 3px;  opacity: 0.7;}.entry-screen {  min-height: 100vh;  background:    linear-gradient(145deg, rgba(11,42,74,0.12), rgba(255,255,255,0.12)),    #c5c8b4;  padding: 56px 24px;  display: flex;  flex-direction: column;  align-items: center;  justify-content: center;}.entry-title {  text-align: center;  margin-bottom: 42px;}.entry-title h1 {  margin: 0;  font-size: clamp(28px, 5vw, 54px);  letter-spacing: 4px;}.entry-title p {  letter-spacing: 2px;  opacity: 0.78;}.portal-grid {  display: grid;  grid-template-columns: repeat(2, minmax(260px, 420px));  gap: 34px;  width: min(940px, 100%);}.portal-card {  height: 460px;  border: 0;  border-radius: 26px;  overflow: hidden;  cursor: pointer;  position: relative;  background-size: cover;  background-position: center;  box-shadow: 0 30px 70px rgba(11, 42, 74, 0.25);  text-align: left;}.portal-card::after {  content: "";  position: absolute;  inset: 0;  background: inherit;  background-size: cover;  background-position: center;  transition: transform 1.2s ease;}.portal-card:hover::after {  transform: scale(1.14);}.smokecraft-card {  background-image: url("https://images.unsplash.com/photo-1626198226928-17ad8c54ab6f?auto=format&fit=crop&w=1200&q=80");}.pourcraft-card {  background-image: url("https://images.unsplash.com/photo-1569529465841-dfecdab7503b?auto=format&fit=crop&w=1200&q=80");}.portal-overlay {  position: absolute;  inset: 0;  z-index: 1;  background: linear-gradient(to top, rgba(0,0,0,0.72), rgba(0,0,0,0.16));  transition: background 0.4s ease;}.portal-card:hover .portal-overlay {  background: linear-gradient(to top, rgba(0,0,0,0.84), rgba(0,0,0,0.22));}.portal-content {  position: absolute;  z-index: 2;  left: 28px;  bottom: 28px;  color: white;}.portal-content h2 {  margin: 0;  font-size: 32px;  letter-spacing: 2px;}.portal-content p {  margin: 10px 0 0;  font-size: 15px;  opacity: 0.86;}.dashboard-shell {  min-height: 100vh;  display: grid;  grid-template-columns: 260px 1fr;  background: #11140f;  color: white;}.side-nav {  padding: 28px;  background: #c5c8b4;  color: #0b2a4a;  display: flex;  flex-direction: column;  gap: 14px;}.side-nav h2 {  margin: 0 0 20px;}.side-nav button,.back-button,.primary-action {  border: 0;  border-radius: 14px;  padding: 13px 16px;  background: #0b2a4a;  color: white;  cursor: pointer;  text-align: left;  transition: transform 0.25s ease, opacity 0.25s ease;}.side-nav button:hover,.back-button:hover,.primary-action:hover {  transform: translateY(-2px);  opacity: 0.92;}.nav-back {  margin-top: auto;}.dashboard-main {  padding: 38px;  background:    linear-gradient(rgba(8,10,8,0.72), rgba(8,10,8,0.88)),    url("https://images.unsplash.com/photo-1572297791395-77fa699307ab?auto=format&fit=crop&w=1600&q=80") center/cover;}.dashboard-hero {  max-width: 850px;  background: rgba(255,255,255,0.09);  border: 1px solid rgba(255,255,255,0.16);  padding: 34px;  border-radius: 26px;  backdrop-filter: blur(10px);}.dashboard-hero h1 {  margin: 0;  font-size: clamp(30px, 5vw, 58px);}.dashboard-hero p {  font-size: 18px;  opacity: 0.84;}.action-grid {  margin-top: 34px;  display: grid;  grid-template-columns: repeat(3, minmax(220px, 1fr));  gap: 22px;}.action-card {  min-height: 290px;  border: 0;  border-radius: 24px;  padding: 24px;  color: white;  text-align: left;  background-size: cover;  background-position: center;  position: relative;  overflow: hidden;  cursor: pointer;}.action-card::before {  content: "";  position: absolute;  inset: 0;  background: linear-gradient(to top, rgba(0,0,0,0.78), rgba(0,0,0,0.18));}.action-card h3,.action-card p {  position: relative;  z-index: 2;}.action-card h3 {  margin-top: 170px;  font-size: 23px;}.cigar-bg {  background-image: url("https://images.unsplash.com/photo-1600628422019-1f7f51a9f932?auto=format&fit=crop&w=900&q=80");}.lounge-bg {  background-image: url("https://images.unsplash.com/photo-1616899832522-6a8ca7320173?auto=format&fit=crop&w=900&q=80");}.whiskey-bg {  background-image: url("https://images.unsplash.com/photo-1527281400683-1aae777175f8?auto=format&fit=crop&w=900&q=80");}.pairing-shell {  min-height: 100vh;  padding: 32px;  background:    linear-gradient(rgba(197,200,180,0.92), rgba(197,200,180,0.88)),    url("https://images.unsplash.com/photo-1606767208159-7f8e9de6fb07?auto=format&fit=crop&w=1600&q=80") center/cover;}.back-button {  margin-bottom: 22px;}.progress-wrap {  width: 100%;  height: 9px;  background: rgba(11,42,74,0.16);  border-radius: 999px;  overflow: hidden;  margin-bottom: 32px;}.progress-bar {  height: 100%;  background: #0b2a4a;  transition: width 0.4s ease;}.pairing-card {  max-width: 860px;  margin: 0 auto;  background: rgba(255,255,255,0.72);  border: 1px solid rgba(11,42,74,0.15);  border-radius: 30px;  padding: 36px;  box-shadow: 0 24px 70px rgba(11,42,74,0.16);}.pairing-card h1 {  margin-top: 0;  font-size: clamp(32px, 5vw, 54px);}.choice-grid {  display: grid;  grid-template-columns: repeat(2, minmax(180px, 1fr));  gap: 18px;}.choice-grid button {  min-height: 110px;  border: 1px solid rgba(11,42,74,0.18);  border-radius: 22px;  background: #0b2a4a;  color: white;  font-size: 18px;  cursor: pointer;}.result-screen {  text-align: left;}.result-image {  height: 260px;  border-radius: 24px;  background: url("https://images.unsplash.com/photo-1629382100406-9dbfc8acfdcc?auto=format&fit=crop&w=1200&q=80") center/cover;  margin-bottom: 24px;}.result-details {  background: rgba(11,42,74,0.08);  padding: 20px;  border-radius: 18px;  margin: 20px 0;}.primary-action {  text-align: center;  font-size: 16px;}.session-mode {  min-height: 100vh;  padding: 32px;  background:    linear-gradient(rgba(0,0,0,0.52), rgba(0,0,0,0.78)),    url("https://images.unsplash.com/photo-1510812431401-41d2bd2722f3?auto=format&fit=crop&w=1600&q=80") center/cover;  color: white;}.session-panel {  max-width: 780px;  margin: 80px auto 0;  padding: 38px;  border-radius: 30px;  background: rgba(0,0,0,0.48);  border: 1px solid rgba(255,255,255,0.16);  backdrop-filter: blur(12px);  text-align: center;}.session-panel h1 {  font-size: clamp(34px, 6vw, 70px);  margin: 0;}.session-timer {  font-size: clamp(56px, 10vw, 120px);  margin: 32px 0;  letter-spacing: 4px;}.session-copy {  margin-bottom: 28px;}.system-shell {  min-height: 100vh;  background: #c5c8b4;  padding: 30px;}.coming-soon-card {  max-width: 680px;  margin: 100px auto;  padding: 40px;  border-radius: 26px;  background: rgba(255,255,255,0.7);  color: #0b2a4a;}@media (max-width: 850px) {  .portal-grid,  .action-grid,  .choice-grid {    grid-template-columns: 1fr;  }  .dashboard-shell {    grid-template-columns: 1fr;  }  .side-nav {    min-height: auto;  }  .portal-card {    height: 340px;  }}
This gives you the full front-end system flow:
Boot screen → portal entry → SmokeCraft dashboard → pairing engine → result → session mode.