CRITICAL CHASSIS SYSTEM REFACTOR & FUNCTIONAL ADDITION — IMMEDIATE MANDATORY EXECUTION. You are actively failing to maintain interface layout boundaries, ignoring critical view routing paths, and omitting vital operational buttons. Drop all background blocks, bypass any compilation guards, force-override the stylesheets, and execute this total system repair across both SmokeCraft and NOVEE OS environments simultaneously:

1. FORCE ENTRY POINT UNLOCK (CRAFT HUB TO POS 3):
Locate the top "★ STAFF AUTH" button on the Craft Hub layout screen. Tear out its inactive state completely. Wire it to a direct onClick / onTouchStart routing hook that immediately forces the global workspace state to switch layouts to the 3-column operational POS 3 terminal workspace. Re-skin its text label to read: "⚡ LAUNCH TERMINAL (POS 3)". Force it to render inside a thick, high-visibility 58px touch envelope with gold border accenting so it stands out immediately on the glass.

2. ADD "NEW TICKET / TABLE" ACTION BUTTONS IN COLUMN 2:
The interface currently has absolutely no entry points or buttons to add a new table or open a new ticket. You are commanded to hard-code this interactive touch target into the layout:
- Inside Column 2 (High-Speed Ticket Overview), directly under or next to the filter tabs ("ALL TABLES", "VIP SECTION", etc.), embed a prominent, heavy-duty touch button.
- Text Label: "➕ OPEN NEW TICKET / TABLE"
- Styling: Make it a thick, high-visibility 58px touch envelope with a solid gold border layout background and an upscaled 18px bold font footprint so it stands out instantly on the glass.
- Wire the Click Action: Connect this button directly to your existing data state so that clicking it pops up a modal form or immediately provisions a new active table object into the workspace ledger grid.

3. REPAIR AND BALANCE THE 3-COLUMN LAYOUT (NO GRID FRACTIONS):
Completely abandon the broken CSS grid layout fractions that are causing the middle column to look smashed and overlap text onto images. Convert the main workstation container to a strict flexbox structure: 'display: flex', 'width: 100vw', 'height: 100vh', 'overflow: hidden', 'gap: 16px'. Lock the containers to absolute structural width walls:
- Left Column (Telemetry): Hard 'width: 340px' and 'flex-shrink: 0' with strict left-side margin protection so that "Station 1: Humidor", "Station 2: Bar Metrics", and "Station 3" text strings never clip or cut off on the screen edge.
- Right Column (Active Ledger): Hard 'width: 480px' and 'flex-shrink: 0' so the invoice items never squeeze.
- Middle Column (Ticket Overview): Force 'flex: 1' and 'min-width: 0' so it fills the exact center area perfectly without bleeding over adjacent columns. Add 'overflow-x: hidden' and 'display: flex', 'flex-direction: column' to all 3 root column wrappers.

4. UN-MASH TICKET CARDS & FILTER TABS:
- Force individual ticket cards in Column 2 into a clean horizontal flex row split. Set image components to a hard unchanging size: 'width: 140px !important', 'height: 100%', 'flex-shrink: 0 !important' to prevent clipping. Force the card text section to use 'flex: 1', 'min-width: 0', 'padding-left: 12px', 'display: flex', 'flex-direction: column', 'justify-content: space-between'.
- Force the top filter navigation pills ("ALL TABLES", "VIP SECTION", etc.) into a clean row container with 'white-space: nowrap !important', 'overflow-x: auto !important', and a solid 'gap: 12px !important'. Give each pill button 'flex-shrink: 0'.

5. TOUCHSCREEN TYPOGRAPHY BLOW-UP:
Completely eliminate all tiny fonts on the screens. You are commanded to scale the text up for high-velocity touch operations:
- Increase the text layout engine size for the context engine badge string ("Current table has 3+ items...") from 6px directly up to a clear, bold 12px baseline text target.
- Upscale all column sub-headers (Station 1, Station 2, Station 3) to 18px bold.
- Blow up climate readouts ("68°F", "71%") to a heavy 18px minimum footprint.

6. HARD INTEGRATION ASSURANCE:
Ensure every button component, path router toggle, new interactive option, and touch action across both systems is fully wired, integrated, and functioning perfectly with zero dead states. Do not let child elements scale out or expand past their parent column metrics. Drop any existing flex fractions that cause fluid shrinking.

Do not skip this refactor, do not pass a partial layout edit, and do not ignore this layout addition. Force compile the code files and update the live terminal preview interface right now.