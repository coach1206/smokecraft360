=======================================================================
MASTER TECHNICAL SPECIFICATION & COMPREHENSIVE REBUILD DIRECTIVE
SYSTEM ENGINE: VISUAL EXPERIENCE ENGINE (VEE) FOR NOVEE OS + E.A.T STACK
PRODUCTION DEPLOYMENT MATRIX · VERSION 5.0.0-PROD · ZERO-PLACEHOLDER INJUNCTION
=======================================================================

You are to execute a complete architectural transformation of the NOVEE OS + E.A.T System from a static user interface shell into an automated, inventory-aware, real-time Visual Experience Engine (VEE). This specification details the unified data layers, automated asset manipulation systems, real-time synchronization buses, and interface rendering pipelines required to build a living cinematic luxury hospitality intelligence platform.

All placeholder icons, static placeholder containers, un-mapped graphics, and generic typographic glyph blocks must be completely decoupled and eliminated from production memory.

---

## SECTION 1: GLOBAL LAYOUT GEOMETRY & TYPOGRAPHIC RESOLUTION

To eliminate the text-collision bugs, overlapping header layout rows, and compressed tracking baselines visible on the current active display hardware, you must first apply strict typographic rendering constraints across the primary viewport shells.

### 1.1 Structural Dual-Header Geometry Layout
* **Top Execution Strip (`.novee-core-header`):** Lock the absolute top system header containment block to a rigid `height: 32px;` profile. Background color must compile to Solid Obsidian (`#010101`). Font specification must map exclusively to `font-family: 'SF Pro Mono', monospace; font-size: 11px; letter-spacing: +1.5px;`. Center-align the status telemetry parameters.
* **Sub-Navigation Horizon Strip (`.eat-navigation-bar`):** Position the secondary luxury operational sub-navigation header block immediately beneath the primary execution strip. Force its geometric footprint to an unyielding `height: 48px;` boundaries. Set the background layer to Satin Chrome (`#121214`).
* **Collision Prevention Grid Rules:** The interaction target parameters ("Command Center", "Environment", "Assets", "Transactions", "Pairing Engine", "Lounge Controls", "Analytics", "Staff Cockpit") must be structured inside a dedicated flexbox matrix using the layout rules below to guarantee clean separation and zero text layering anomalies:
    ```css
    .eat-navigation-bar .nav-item-link-group {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 24px;
        gap: 24px;
    }
    .eat-navigation-bar .nav-item-link {
        font-family: 'Inter Display', sans-serif;
        font-size: 14px;
        font-weight: 500;
        letter-spacing: +0.5px;
        white-space: nowrap;
        color: #8E8E93;
        transition: color 0.12s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .eat-navigation-bar .nav-item-link:hover, 
    .eat-navigation-bar .nav-item-link.active {
        color: #D4AF37; /* Enforced Honey Amber Active Core Accent */
    }
    ```

### 1.2 Bottom Intelligence Ticker Calibration
* **Padding Constraints:** Force the absolute bottom black background strip (`.eat-intelligence-footer`) to allocate exactly `height: 40px;` of vertical workspace. Apply an inward horizontal padding envelope of `padding: 0 24px;`.
* **Grid Separation:** Structure the telemetry data readouts (`LOUNGE TEMP 68°F`, `HUMIDITY 72%`, `HUMIDOR COUNT 142 Puros`, `LOUNGE MODE Active`, `POS TRANSACTION Authenticated`) within an explicit high-readability row layout. Prevent compression by defining grid distribution limits:
    ```css
    .eat-intelligence-footer .telemetry-grid {
        display: grid;
        grid-template-columns: repeat(5, minmax(160px, 1fr));
        align-items: center;
        width: auto;
        gap: 32px;
        font-family: 'SF Pro Mono', monospace;
        font-size: 11px;
        color: #38A169; /* System Nominal Green Core Code */
    }
    ```

---

## SECTION 2: ARCHITECTURAL DATA CORE SCHEMAS (POSTGRESQL)

To support the inventory-aware asset mapping, metadata categorization, and cross-platform runtime state tracking, you must deploy the relational database tables defined below. All image entities must track strict binary optimization footprints.

```sql
-- Enforce UUID generation extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 2.1 Media Assets Core System Table
CREATE TABLE media_assets (
    asset_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    file_name VARCHAR(255) NOT NULL,
    storage_url TEXT NOT NULL,
    thumbnail_url TEXT NOT NULL,
    mime_type VARCHAR(64) NOT NULL,
    file_size_bytes INTEGER NOT NULL,
    category_tag VARCHAR(32) CHECK (category_tag IN ('CIGAR', 'BOURBON', 'WHISKEY', 'COCKTAIL', 'FOOD', 'PAIRING_SCENE', 'LOUNGE_ATMOSPHERE', 'STAFF_IMAGERY', 'EVENT_PROMOTION', 'VIP_ROOM', 'LIGHTING_SCENE', 'MUSIC_THEME', 'FLAVOR_VISUAL')),
    flavor_tags VARCHAR(64)[] DEFAULT '{}',
    atmosphere_tags VARCHAR(64)[] DEFAULT '{}',
    metadata_json JSONB DEFAULT '{}'::jsonb,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 2.2 Pairing Visuals Aggregation Table
CREATE TABLE pairing_visuals (
    pairing_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    cigar_product_sku VARCHAR(64) NOT NULL,
    liquor_product_sku VARCHAR(64) NOT NULL,
    food_product_sku VARCHAR(64) DEFAULT NULL,
    generated_scene_url TEXT NOT NULL,
    fallback_generated_boolean BOOLEAN DEFAULT FALSE,
    composition_metadata JSONB DEFAULT '{}'::jsonb,
    last_rendered_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 2.3 Generated Scenes Cache Ledger
CREATE TABLE generated_scenes (
    scene_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    prompt_payload TEXT NOT NULL,
    image_base64_payload TEXT DEFAULT NULL,
    cached_cdn_url TEXT NOT NULL,
    associated_atmosphere_id VARCHAR(64) NOT NULL,
    generation_metrics_json JSONB DEFAULT '{}'::jsonb,
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL
);

-- 2.4 Inventory Image Mappings Record
CREATE TABLE inventory_images (
    mapping_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    product_sku VARCHAR(64) UNIQUE NOT NULL,
    primary_media_id UUID REFERENCES media_assets(asset_id) ON DELETE SET NULL,
    secondary_alpha_png_id UUID REFERENCES media_assets(asset_id) ON DELETE SET NULL,
    is_active_in_vault BOOLEAN DEFAULT TRUE,
    last_synchronized_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 2.5 Venue Ambient Background Configurations
CREATE TABLE venue_backgrounds (
    background_id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    venue_identifier VARCHAR(64) NOT NULL,
    zone_id VARCHAR(64) NOT NULL,
    atmosphere_profile_slug VARCHAR(32) NOT NULL,
    base_layer_media_id UUID REFERENCES media_assets(asset_id),
    smoke_overlay_intensity NUMERIC(3,2) DEFAULT 0.50,
    amber_glow_coefficient NUMERIC(3,2) DEFAULT 0.85
);