DO NOT hardcode credentials or PINs into frontend TypeScript files.

Use environment secrets and the existing authentication architecture.

IMPLEMENTATION RULES:

1. USE EXISTING AUTH SYSTEM
   Hook the SovereignGate into the current JWT/staff authentication system already present in the platform.

DO NOT create standalone hardcoded authentication.

2. STORE ALL SENSITIVE VALUES IN ENV
   Use:

* .env
* Replit Secrets
* server-side encrypted values

Examples:

* FOUNDER_PIN_HASH
* EEIE_MASTER_KEY
* ADMIN_OVERRIDE_TOKEN

Never expose these in frontend code.

3. ALL PIN VALIDATION MUST HAPPEN SERVER-SIDE
   Create:
   POST /api/sovereign/validate

Flow:
Frontend submits entered PIN →
Server validates against hashed secret →
Server returns signed temporary session token →
Frontend unlocks EEIE/Sovereign layer

NO frontend-only validation.

4. USE HASHED PIN STORAGE
   Use:
   bcrypt or argon2

DO NOT store plain-text PINs anywhere.

5. ROUTING STRUCTURE
   Routes should EXIST INSIDE the current app architecture.

Protected routes:

* /gate
* /eeie
* /sovereign-dashboard
* /admin-master

These should use:
ProtectedRoute / RoleGuard architecture.

NOT standalone disconnected pages.

6. CORRECT PROJECT PATHS
   All implementation paths must follow the actual project structure:

artifacts/smokecraft/src/

NOT:
src/

Do NOT generate orphaned files outside the live render tree.

7. EEIE ACCESS FLOW
   Correct flow:

Sovereign Button →
Gate Overlay →
PIN Entry →
POST /api/sovereign/validate →
temporary signed session →
mount EEIE overlay →
activate handoff

8. SECURITY REQUIREMENTS

* Founder routes must require elevated role
* Temporary EEIE sessions auto-expire
* Audit all Sovereign access attempts
* Rate-limit PIN attempts
* Lockout after repeated failures
* Log all kill-switch activations

9. DO NOT STOP AT SECURITY WARNINGS
   Proceed with:

* SovereignGate
* PhantomHUD
* AmbassadorWizard
* EEIE routing
* protected overlays
* handoff architecture

using secure implementation practices.

EXECUTE ALL FOUR SYSTEMS IMMEDIATELY USING THE EXISTING AUTH ARCHITECTURE.
