CRITICAL CRAFTHUB ARCHITECTURE OVERHAUL & UNIFIED REVENUE ECOSYSTEM CODE INJECTION: The codebase is riddled with 47 critical gaps and lazy shortcuts. You must immediately refactor App.tsx, MasterBlender.tsx, RitualStep.tsx, usePosIntegration.ts, and all contextual layout, validation, and routing components. You are strictly forbidden from using vertical flex-column stacking, flat text rows, web-style dead margins, or empty feature flags. All four craft pillars must share identical visual physics, scoring loops, backend ledger hooks, and multi-visit locks. Verify 'tsc --noEmit' passes perfectly with exit code 0.

1. BRAND PURGE & THE SYMMETRICAL LANDSCAPE COCKPIT GRID
   - Completely delete VapeCraft.tsx, remove the /vape routing rules, and wipe any reference to vapes from the system architecture. In its place, deploy WINE_CRAFT_360 as the permanent fourth pillar.
   - Refactor CraftHub.tsx to permanently eliminate 'flexDirection: "column"' properties on main container decks. Force the landing view into an edge-to-edge landscape widescreen tablet viewport:
     * SMOKECRAFT 360 (Top Component): Permanently fixed across the upper boundary of the display, filling 100% of the horizontal layout workspace.
     * POURCRAFT 360, BEERCRAFT 360, WINECRAFT 360 (Bottom Row Component): Grouped side-by-side neatly directly beneath SmokeCraft, split into three mathematically equal columns.
   - Haptic & Acoustic Physics Engine: Abstract and elevate touch tactility globally. Wrap all interactive targets across all four crafts in a uniform touch handler that fires a 100ms micro-compression (scale-96 combined with a vertical offset of translate-y-[2px]) on direct glass contact, synchronized with an acoustic 3400Hz mechanical button-reset click burst via the Web Audio API.
   - Universal Visual Motion Mandate: No page is allowed to render raw, un-styled text rows or static cards. Every view must run active dark-slate background depths with custom animated particle movement matching the craft domain:
     * SmokeCraft: Slow-drifting HTML5 ash smoke mist & low-heat burning embers.
     * PourCraft: Swirling rich amber liquor light refractions & dense fluid waves.
     * BeerCraft: Rising micro-oxygen carbonation bubbles & golden hops frost.
     * WineCraft: Deep velvet wine legs rolling slowly down dark, backlit glass.

2. ENTRY-PORTAL 5-MILE PROXIMITY ENGAGEMENT & REVENUE TICKER TAPE
   - The exact fraction of a second a user selects any craft module from the main cockpit grid, intercept the workflow with a glassmorphic location access prompt.
   - Wire an explicit Opt-In/Opt-Out toggle switch bound directly to their mandatory mobile phone number entry. The UI text must read exactly: "Opt-in to unlock exclusive, immediate lounge table perks, flash discounts, and local neighborhood tasting updates."
   - Radius Tracking Loop: For opted-in guests, log broad coordinate markers. When their device registers within a strict 5-mile radius of the lounge's GPS coordinates, their context turns to Active inside the marketing ledger.
   - Slow-Day SMS Broadcaster: Build an API action trigger inside the E.A.T. management dashboard allowing operators to dispatch immediate, targeted text alerts (flash happy hour drink specials, limited humidor clear-outs, or kitchen promotions) straight to nearby opted-in phone numbers to immediately accelerate tableside traffic.
   - Dynamic Ticker Tape Ribbon: Install a scrolling, horizontal marquee ribbon gliding right above the bottom screen boundary across all active phases. It must continuously loop live venue drink specials, kitchen item prompts, and luxury paid partner ad placements.

3. THE STATIONARY GOLDEN BOX GATE & FULL DEMOGRAPHIC CAPTURE
   - Craft Interstitial Gateway Views: The introduction screen for any craft must hold perfectly static on the glass layout with zero automatic setTimeout routing bypasses.
   - The Scoring Disclosures: Explicitly write the rules of "The Golden Box" into the component state: teaching guests how they earn +5 XP achievements for matching component chemistry, how they are docked -2 PTS by real-time mentor interventions for regional contradictions, and display the active Table Spend-to-Points Multipliers:
     * Table Spend $50.00 – $99.99  --> Instantly adds +10 XP to their running total.
     * Table Spend $100.00 – $199.99 --> Instantly adds +25 XP to their running total.
     * Table Spend $200.00+          --> Instantly adds +60 XP Elite Vault Badge.
   - Full Demographic Capture Form: Overlaid directly on the active particle canvas is the multi-column roundtable form. New users must populate all parameters to unlock the gold "BEGIN JOURNEY" action block: Full Name, Mobile Phone, Email, Age Range dropdown (21-30, 31-45, 46-60, 61+), Gender Selection toggles (M / F / Prefer Not to Say), State selection dropdown, and City text input field.

4. SHARED 2-STEP RETURNING LOGIN & CHECKPOINT STATE RE-HYDRATION
   - Abstract the return login logic completely out of SmokeCraft and turn it into a global shared identity service module.
   - Returning masterclass guests across any craft bypass the long onboarding forms by tapping "Returning Masterclass Guest." Render a clean, centered verification card requiring only two inputs: Last Name and the Last 4 Digits of their Phone Number.
   - Upon verification, fetch the guest's profile JSON data, read the persistent 'phase_checkpoint' field in the database ledger, and leap the user directly onto their exact saved phase screen—completely skipping the entry overviews and introductory chapters.

5. HARD 3-VISIT LIFECYCLE SEPARATION & IMMUTABLE PROGRESSION LOCKS
   - Segment the interactive phases of all four crafts across exactly 3 separate physical lounge visits to protect development scope and drive recurring tableside revenue:
     * VISIT 1: Onboarding Gate -> Craft Overview -> Mentor Selection & Interactive AI Chat Box -> Foundation Sourcing Challenge -> REVENUE-DRIVEN SUMMARY GATE -> HARD SESSION STANDBY FREEZE.
   - The Interactive AI Mentor Chat Box: Step 3 across all crafts must feature a widescreen selector displaying 3 luxury card columns with explicit national flags (SmokeCraft: Don Manuel DR 🇩🇴, Alejandro NIC 🇳🇮, Doña Rosa ECU 🇪🇨 | PourCraft: Master Macallan SCO 🏴󠁧󠁢󠁳󠁣󠁴󠁿, Don Julio MEX 🇲🇽, Elijah Craig USA 🇺🇸 | BeerCraft: Hans GER 🇩🇪, Liam IRE 🇮🇪, Brad USA 🇺🇸 | WineCraft: Jean-Louis FRA 🇫🇷, Matteo ITA 🇮🇹, Santiago ARG 🇦🇷) tied to ultra-realistic portrait render viewports. Selecting a mentor triggers an ElevenLabs vocal track and slides a custom text chat box into focus. The mentor asks if they have any questions, allowing the guest to type or voice-trigger craft-related questions and receive context-aware, expert dialogue responses. Include a global MUTE switch.
   - Visit 1 Hard Stop Protocol: The exact second a guest resolves the Visit 1 Foundation Sourcing Challenge (SmokeCraft: Soil Terroir | PourCraft: Grain & Water Minerals | BeerCraft: Base Roasted Malts & Hops | WineCraft: Grape Genetics & Vineyard Terroir), the router is strictly forbidden from advancing into subsequent phases. Visit 1 ends here.
   - Intercept with the Milestone Summary Gate: Animate their running score odometer face-to-face against the venue's active Nightly Lounge Average baseline. Query the backend database tables in real-time to display a high-fidelity graphic asset of an authentic product currently in stock matching their choice (SmokeCraft: Padrón 1926/Opus X | PourCraft: Macallan 18/Don Julio 1942 | BeerCraft: Allocated Stout/Craft IPA | WineCraft: Allocated Bordeaux/Napa Cab) paired inline with a premium beverage or food up-sell special available for immediate tableside ordering.
   - Standby Lockout: Commit all profile analytics, demographics, scores, and checkpoint values to the persistent server row under their phone number context. Gracefully terminate the active module flow and reset the kiosk back to the primary CraftHub portfolio dashboard—blocking subsequent stages until they return to a lounge console on a future visit.

6. OPERATIONAL TELEMETRY, MANAGER PIN OVERRIDES, & BACKGROUND LEDGERS
   - Continuous Behavioral Consumption Ledger: While the terminal holds in standby or during live interaction, silently write operational tracking fields directly to the customer's database row: logging exact smoking/sitting times, total drink counts, food ordering velocity, and historical flavor affinity data.
   - Secure E.A.T. Staff Handoff Layer: Tapping a designated hidden corner touch zone prompts a secure Staff PIN overlay. Authorized entries instantly execute a clean 180-degree vertical screen-flip animation, masking the customer space and populating the three pillars of the E.A.T. (Environment, Asset, Transaction) Dashboard side-by-side:
     * E (Environment Panel): Displays device connection stability, hardware touch telemetry, and active canvas rendering health.
     * A (Asset Panel): Displays a live window into back-bar fluid ounces, kitchen ticket lines, and humidor stock levels. Features your interactive Drag-and-Drop Floor Map Configurator and Seating Arranger allowing staff to visually arrange, scale, and label custom room shapes, table, and bar seating layouts straight to the central database repository.
     * T (Transaction Panel): Displays customer profiles, active score values, total tableside spend records, and live drink counts. Staff can apply manual score adjustments or clear point overrides here.
     * Tapping 'Resume Guest Journey' collapses the dashboard, vertically flips back, and instantly re-hydrates the guest's cached snapshot exactly where they left off.
   - Permanent Day One 360 Sponsorship Integration: Day One 360 commands absolute placement in the global layout header across all four crafts alongside the NOVEÈ OS signature. Tapping it slides up a corporate travel and relocation drawer without breaking the session state. The system injects a unique tracking identifier (kiosk_terminal_id) into every lookup request. When a guest completes a relocation or flight itinerary, log the conversion metadata and automatically credit a 10% commission payout share straight back to the venue’s ledger balance.
   - Rolling Hardware Idle Timeout: A 15-minute active session timer runs continuously. If no screen touch contact is detected for 3 continuous minutes, the display dims and triggers a large, glassmorphic 60-second warning countdown clock. Expiry automatically flushes accumulated metrics to the server database and resets the terminal safely back to the cockpit grid.
   - Geographic Perimeter Geofencing Zone Protection: The tablet terminal constantly monitors its network geolocation packet state. If a tablet device is physically moved outside its assigned venue perimeter coordinates, background security controllers immediately sever all staff notification loops, clear active management PIN data caches from temporary memory, lock down backend API access tokens, and force a full-screen absolute blackout security layout until it is safely returned to its authorized station.
   - Cross-Fragmented Inventory & Predictive Reorder Core: Actively unifies your fragmented on-site databases—linking the kitchen POS tickers, the main back-bar liquid volume meters, and the climate-controlled humidor counters into a single ledger logic block. The fraction of a second an asset tier dips below its pre-configured low-stock threshold, the system automatically generates a unified B2B supply purchase order sheet, pushes the data sync directly to your distributors' portals, and logs the pending delivery straight onto the management inventory calendar.

Execute this comprehensive entry-sequence configuration, side-by-side craft portfolio layout alignment, mentor chat box construction, revenue metrics integration, and multi-visit session freeze pass now.