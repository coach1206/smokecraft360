{
  "widgetSpec": {
    "height": "800px",
    "prompt": "**Objective:** Create an interactive, enterprise-grade 2D Gestural Engine and Fluid Motion Simulator for a touch-first POS and E.A.T. (Environment, Asset, Transaction) Ecosystem. The interface must fully manifest a ultra-luxury, high-performance aesthetic, utilizing a True Obsidian (#010101) base, deep charcoal glass panels (#111111), and precise amber/gold (#D4AF37) highlights. The application must accurately simulate fluid touch dynamics, muscle-memory shortcuts, asynchronous edge queuing, and continuous spatial physics—completely eliminating traditional static modals and desktop-centric layout patterns.\n\n**Data Architecture & Core State:**\n1. `activeItems`: Array of active high-end hospitality asset transactions. Each item must track ID, Name, Category (Asset / Environment / Transaction), Unit Price, Local Quantity, Sync Status (`'Synced'`, `'Syncing'`, `'Queued'`), Split State (Boolean / Split Percentage), and a localized multi-axis layout offset (`x`, `y`).\n   - *Default Items:* \n     - \"01. Don Julio 1942 (PourCraft)\" | $50.00\n     - \"02. Amber Aura Ritual (SmokeCraft)\" | $75.00\n     - \"03. Opus One Cabernet (WineCraft)\" | $120.00\n2. `asyncQueue`: Array monitoring background network sync frames, logging actions, execution timestamp, injected simulated network latency, and current transmission payload status.\n3. `ambientHUD`: State object for live environmental telemetry metrics including Smoke Density (%), Audio Amplitude (dB), and Color Temperature (K).\n4. `gestureEngine`: Active touch metadata registry tracking `touchStartLocation` (x, y), `currentDelta` (x, y), `holdActiveTimer` (ms), and active interpolated physics state (`'Rest'`, `'Lifted'`, `'UnderlayOpened'`, `'Tearing'`).\n\n**Layout & UI Grid Strategy (Split-Viewport System):**\n- **Left Side (The Tactile Workspace Canvas):** Mimics an ultra-premium, zero-bezel physical touchscreen tablet interface embedded into a luxury venue bar or console. Features a live canvas container populated with dynamic, touch-reactive asset cards.\n- **Right Side (The Telemetry & Edge Control Engine):** A dense, technical diagnostics dashboard displaying active physics curves, live interpolation formulas, touch coordinate vectors, and the asynchronous edge transmission logs.\n\n**Interactive Input Modules & Gestural Engines:**\n1. **Dynamic Item Card Workspace Controllers:**\n   - **Velocity-Based Horizontal Swipe Right (Increment Engine):** Swiping right on an asset card stretches the container with elastic deformation. Passing a specific velocity/pixel threshold immediately registers an instant $+1$ quantity addition locally on the UI ($0ms$ apparent interface latency) while spawning an asynchronous background network request frame into the sync queue.\n   - **The Obsidian Underlay Slide Left (Hidden Contextual Modifiers):** Swiping left pulls away the top item layer like heavy plate glass, smoothly decelerating via an inertia curve to reveal a deep-set underlay row containing crisp micro-action toggles (*Comp/Void*, *Route Asset*, *Tag Zone*). Tapping outside or swiping back instantly reverses the physics calculation.\n   - **The Dual-Finger Split/Merge Emulator (Pinch & Stretch):** Special action handlers on each card simulating multi-finger manipulation. Clicking \"Simulate Split Tearing\" triggers a multi-finger tear animation where the asset card splits cleanly down its center line, splitting its value precisely into two separate, independent child containers ($50\%$ value attribution) with fluid organic scaling. Tapping \"Simulate Fusion\" pools adjacent item boundaries back into a singular, calculated block.\n   - **Tactile Press & Hold (300ms Weight Threshold):** Pressing and holding an item container for over 300ms enters **Weight Mode**. The specific item scales up ($+3\%$) and projects a wide, diffused atmospheric drop shadow to simulate a physical mechanical lift off the interface plane, while the remaining canvas elements gracefully dim by 15% to lock complete operator focus.\n2. **Network Interceptor Configuration Sliders:**\n   - **Simulated Transit Latency Regulator:** Adjusts artificial back-end latency from $0ms$ (instant localhost) up to $4000ms$ (severe network drop). This proves the Asynchronous Fail-Safe: operators can frantically tap and wipe to modify items at lightspeed, and the UI remains completely fluid while stacking background queues seamlessly without frozen screens or blocking load spinners.\n\n**Visual Styling, Physics Tuning & Environmental Feedback Curves:**\n- **True Obsidian Aesthetics:** Background canvas locked at absolute deep dark `#010101`. Control modules styled as charcoal glass blocks (`#111111`) with sharp, high-contrast, minimalist 1px amber or dark gray borders. Typography must utilize stark, monospaced tech lettering for data, and ultra-clean minimalist sans-serif for operations.\n- **Ambient UI Perimeter Health Frame:** The outermost borders of the primary touchscreen canvas function as a continuous live status indicator. Ambient data changes (e.g., severe depletion of a sensory fluid asset or extreme system environment values) cause this razor-thin border to pulse smoothly at varying frequencies using low-impact color shifts, giving operators complete peripheral situational awareness without occupying central display zones.\n- **Exact Fluid Physics Profiles:** All programmatic animation loops must run on precise, non-linear timing parameters derived from the system engineering specification:\n  - *Lift / Select:* $150ms$ duration via `cubic-bezier(0.2, 0.8, 0.2, 1)` for immediate, snappy finger-snapping response.\n  - *Underlay Reveal:* $250ms$ duration via `cubic-bezier(0.16, 1, 0.3, 1)` mirroring heavy plate glass slide inertia.\n  - *Elastic Snap-Back:* $350ms$ duration via `cubic-bezier(0.34, 1.56, 0.64, 1)` introducing a micro-dampened organic bounce when an interaction is canceled.\n- **Live Metric Visualization Dashboard:** The right-hand telemetry board must display real-time interactive graphs or numeric logs showing current active Touch Coordinates $(x, y)$, immediate Velocity Vectors, Active Physics Interpolation Constants, and a live waterfall tracker monitoring pending asynchronous payloads moving through the local edge storage layers."
  }
}