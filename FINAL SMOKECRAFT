MISSING / REQUIRED

1. public/stitch-export.html is live, but it is only a standalone static Stitch export.
Impact: The page displays visually, but the cards, buttons, sidebar items, and screen interactions are dead because they are not wired into the React app.

2. The Stitch export must be converted into real React components.
Impact: The visual screen needs to become part of the actual SmokeCraft 360 / NOVEE OS application, not a loose HTML page.

3. The clickable areas must be wired.
Impact: SmokeCraft 360, WineCraft, PourCraft, BeerCraft, Concierge, Cellar, Humidor, Lounge, History, Settings, and Summon Staff need actual click behavior.

4. The route system must be connected.
Impact: Users must be able to move from the CraftHub Portal into each craft experience and return with back/home navigation.

5. The original public/stitch-export.html must be kept as backup.
Impact: Do not delete the working visual reference until the React version is fully rebuilt.

FAILURE PREVENTION CHECKLIST

Do not delete public/stitch-export.html.
Do not delete App.jsx.
Do not delete App.css.
Do not overwrite the current app blindly.
Do not break GitHub Pages deployment.
Do not remove any existing routes.
Do not replace the Stitch design with a generic page.
Do not use emoji, glyph icons, or clipart.
Do not leave dead buttons.
Do not create fake navigation that only changes text without changing screens.
Do not leave console errors.
Do not break mobile or tablet layout.
Do not remove the premium dark cinematic CraftHub look.
Do not remove the sidebar.
Do not remove the curator/profile section.
Do not remove the card imagery.
Do not remove hover effects, motion, or visual depth.

MASTER PROMPT

You are working inside the existing GitHub repository for SmokeCraft 360 / NOVEE OS.

The file public/stitch-export.html was added from Stitch and is now live, but it is a static standalone page. Convert it into a real functional React experience inside the existing app.

Task 1: Audit the current structure

Review these files first:

App.jsx
App.css
package.json
public/stitch-export.html
any existing components, pages, routes, assets, or style files

Determine whether the app currently uses React Router or simple state-based screen switching.

Task 2: Create a real React page

Create a new React component named:

CraftHubVisualPortal.jsx

Place it in the correct directory based on the existing project structure. If there is no pages folder, create:

src/pages/CraftHubVisualPortal.jsx

If this project uses root-level App.jsx only, create:

components/CraftHubVisualPortal.jsx

Convert the visual structure from public/stitch-export.html into this React component.

Preserve:
dark cinematic layout
left sidebar
curator profile block
CraftHub Portal headline
search bar
notification/settings buttons
large craft cards
SmokeCraft 360 card
WineCraft card
PourCraft card
BeerCraft card
Concierge sidebar state
premium amber/black styling
hover effects
responsive layout
touchscreen-friendly sizing

Task 3: Wire the clicks

Make the following elements clickable:

SmokeCraft 360 card
WineCraft card
PourCraft card
BeerCraft card
Cellar
Humidor
Lounge
Concierge
History
Settings
Summon Staff
Search bar
Notification icon
Settings icon

If matching existing routes already exist, route to them.

If routes do not exist, create placeholder React screens with premium styling and a clear message that the module is ready for backend wiring.

Required routes or screen states:

/craft-hub
/smokecraft
/winecraft
/pourcraft
/beercraft
/cellar
/humidor
/lounge
/concierge
/history
/settings
/staff-summon

Each page must include:
Back button
Home button returning to /craft-hub
Premium dark visual style
No dead buttons
No generic blank screens

Task 4: Add routing

If React Router is installed, add proper routes.

If React Router is not installed, install and configure react-router-dom, or use the existing navigation method if the app already has one.

Make sure the CraftHub visual portal is accessible at:

/craft-hub

Also add a fallback route so users do not land on a blank page.

Task 5: Preserve the standalone file

Keep:

public/stitch-export.html

Do not delete it.

Add a comment in the code that it is a backup Stitch export reference.

Task 6: Fix asset paths

Check all image paths inside the Stitch export.

If images are external URLs, keep them working.

If images are local paths, move them into public/assets or the correct existing assets folder.

Make sure no cards show broken images.

Task 7: Add real interactive behavior

Add:
hover glow on cards
active sidebar state
basic search input behavior
click feedback
staff summon modal
settings modal or settings page
history placeholder list
concierge mode panel
card selection animation
responsive behavior for desktop, tablet, and kiosk

Task 8: Keep build clean

Run the project build.

Fix all errors.

Make sure:
npm install works
npm run dev works
npm run build works if configured
GitHub Pages does not break

Required output after work is complete:

1. List every file changed.
2. Confirm the new route added.
3. Confirm which buttons are now clickable.
4. Confirm if public/stitch-export.html was preserved.
5. Confirm if any assets were moved or fixed.
6. Confirm the exact command to run the app.
