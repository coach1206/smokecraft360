import React from 'react';

/**
 * AXIOM 360 - SOVEREIGN TERMINAL OVERRIDE
 * FORCES TALL GEOMETRY | FORCES BRIGHT IMAGES | FORCES 24K GOLD LUSTER
 */

const AxiomTerminal = () => {
  const styles = {
    // LOCK THE FRAME: No scrolling, no "website" feel
    shell: {
      height: '100vh',
      width: '100vw',
      backgroundColor: '#050505',
      background: 'radial-gradient(circle at 50% 0%, #1c1c1e 0%, #050505 100%)',
      color: '#fff',
      display: 'flex',
      flexDirection: 'column',
      padding: '50px',
      boxSizing: 'border-box',
      overflow: 'hidden',
      position: 'relative',
    },
    // THE 24K GOLD LUSTER: Engraved Metallic Shine
    gold: {
      background: 'linear-gradient(180deg, #fff9e6 0%, #d4af37 45%, #8a6d3b 100%)',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      fontWeight: '900',
      letterSpacing: '0.4em',
      filter: 'drop-shadow(0 0 15px rgba(212, 175, 55, 0.6))',
      textTransform: 'uppercase',
    },
    // THE TALL SLABS: High-Contrast Glass Panels (75% of screen height)
    slab: {
      flex: '1',
      height: '75vh', 
      borderRadius: '45px',
      position: 'relative',
      overflow: 'hidden',
      borderTop: '3px solid rgba(255, 255, 255, 0.3)', 
      borderLeft: '1px solid rgba(255, 255, 255, 0.1)',
      boxShadow: '0 60px 120px rgba(0,0,0,1)',
      background: '#121214',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      padding: '50px',
    },
    hero: {
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      zIndex: 0,
      filter: 'brightness(1.4) contrast(1.1)', // This KILLS the dark veil
    },
    overlay: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to top, rgba(0,0,0,0.95) 0%, transparent 60%)',
      zIndex: 1,
    }
  };

  return (
    <div style={styles.shell}>
      {/* HEADER: STATIONARY HARDWARE LABEL */}
      <header style={{ zIndex: 10, display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '2px solid rgba(255,255,255,0.05)', paddingBottom: '35px' }}>
        <div>
          <h1 style={{ ...styles.gold, fontSize: '38px', margin: 0 }}>Axiom 360</h1>
          <p style={{ fontSize: '11px', letterSpacing: '0.7em', opacity: 0.4, marginTop: '10px', textTransform: 'uppercase' }}>
            Sovereign OS // Premier Terminal
          </p>
        </div>
        <div style={{ background: 'rgba(212, 175, 55, 0.05)', padding: '15px 45px', borderRadius: '20px', border: '2px solid #d4af37' }}>
          <span style={{ ...styles.gold, fontSize: '14px' }}>STATUS: MASTER RANK</span>
        </div>
      </header>

      {/* MAIN: THE TALL VERTICAL SLABS (BYPASSES THE SMASHED GRID) */}
      <div style={{ display: 'flex', gap: '60px', flex: '1', marginTop: '50px', zIndex: 5 }}>

        {/* SMOKECRAFT SLAB */}
        <div style={styles.slab}>
          <img src="https://images.unsplash.com/photo-1527030280862-64139fba04ca?w=1600" style={styles.hero} alt="Cigar" />
          <div style={styles.overlay}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ ...styles.gold, fontSize: '65px', marginBottom: '15px', fontStyle: 'italic' }}>Smokecraft</h2>
            <p style={{ fontSize: '14px', color: '#fff', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '45px', textTransform: 'uppercase' }}>
              Molecular Leaf Atelier // Ritual Prep
            </p>
            <button style={{ 
              background: 'rgba(0,0,0,0.9)', 
              border: '2px solid #d4af37', 
              padding: '22px 60px', 
              color: '#d4af37', 
              fontWeight: '900', 
              letterSpacing: '0.6em',
              cursor: 'pointer',
              fontSize: '12px'
            }}>INITIATE RITUAL</button>
          </div>
        </div>

        {/* POURCRAFT SLAB */}
        <div style={styles.slab}>
          <img src="https://images.unsplash.com/photo-1544027993-37dbfe43562a?w=1600" style={styles.hero} alt="Pour" />
          <div style={styles.overlay}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ background: 'linear-gradient(to bottom, #fff, #999)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', fontSize: '65px', marginBottom: '15px', fontStyle: 'italic' }}>Pourcraft</h2>
            <p style={{ fontSize: '14px', color: '#fff', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '45px', textTransform: 'uppercase' }}>
              Vessel Geometry // Spirit Sync
            </p>
            <button style={{ 
              background: 'rgba(255,255,255,0.05)', 
              border: '2px solid rgba(255,255,255,0.3)', 
              padding: '22px 60px', 
              color: '#fff', 
              fontWeight: '900', 
              letterSpacing: '0.6em',
              cursor: 'pointer',
              fontSize: '12px'
            }}>ANALYZE POUR</button>
          </div>
        </div>

      </div>

      {/* FOOTER: INDUSTRIAL TELEMETRY */}
      <footer style={{ marginTop: 'auto', display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderTop: '2px solid rgba(255,255,255,0.05)', paddingTop: '40px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '25px' }}>
          <div style={{ 
            width: '22px', 
            height: '22px', 
            background: '#d4af37', 
            transform: 'rotate(45deg)', 
            boxShadow: '0 0 35px #d4af37' 
          }}></div>
          <span style={{ fontSize: '12px', letterSpacing: '0.7em', opacity: 0.4, textTransform: 'uppercase', color: '#fff' }}>
            Titan Engine // Live Telemetry
          </span>
        </div>
        <span style={{ ...styles.gold, fontSize: '11px', opacity: 0.6 }}>Axiom Sovereign OS v4.0</span>
      </footer>
    </div>
  );
};

export default AxiomTerminal;