import React from 'react';

/**
 * AXIOM 360 SOVEREIGN TERMINAL - MASTER ATELIER
 * ALL STYLES ARE INLINED TO BYPASS EXTERNAL CSS CACHING
 */

const AxiomTerminal = () => {
  const styles = {
    // LOCKS THE FRAME: Kills the "website" feel
    shell: {
      height: '100vh',
      width: '100vw',
      backgroundColor: '#050505',
      background: 'radial-gradient(circle at 50% 0%, #1c1c1e 0%, #050505 100%)',
      color: '#fff',
      display: 'flex',
      flexDirection: 'column',
      padding: '40px',
      boxSizing: 'border-box',
      overflow: 'hidden',
      fontFamily: 'sans-serif',
      position: 'relative',
    },
    // 24K GOLD LUSTER
    goldLuster: {
      background: 'linear-gradient(180deg, #fff9e6 0%, #d4af37 45%, #8a6d3b 100%)',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      fontWeight: '900',
      letterSpacing: '0.4em',
      filter: 'drop-shadow(0 0 15px rgba(212, 175, 55, 0.6))',
      textTransform: 'uppercase',
    },
    // STERLING SILVER LUSTER
    silverLuster: {
      background: 'linear-gradient(180deg, #ffffff 0%, #9ca3af 50%, #4b5563 100%)',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      fontWeight: '700',
      letterSpacing: '0.2em',
    },
    // THE SLAB: Forces the "Tall" geometry
    tallSlab: {
      flex: '1',
      height: '70vh', 
      borderRadius: '40px',
      position: 'relative',
      overflow: 'hidden',
      borderTop: '2px solid rgba(255, 255, 255, 0.3)', 
      borderLeft: '1px solid rgba(255, 255, 255, 0.1)',
      boxShadow: '0 50px 100px rgba(0,0,0,0.9)',
      background: '#121214',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      padding: '40px',
    },
    imageLayer: {
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      zIndex: 0,
      filter: 'brightness(1.3) contrast(1.1)', 
    },
    overlay: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to top, rgba(0,0,0,0.95) 0%, transparent 60%)',
      zIndex: 1,
    }
  };

  return (
    <div style={styles.shell}>
      {/* HEADER: STATIONARY TERMINAL */}
      <header style={{ zIndex: 10, display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid rgba(255,255,255,0.1)', paddingBottom: '30px' }}>
        <div>
          <h1 style={{ ...styles.goldLuster, fontSize: '32px', margin: 0 }}>Axiom 360</h1>
          <p style={{ fontSize: '10px', letterSpacing: '0.6em', opacity: 0.4, marginTop: '8px', textTransform: 'uppercase' }}>
            Sovereign Console // Premier Terminal
          </p>
        </div>
        <div style={{ background: 'rgba(255,255,255,0.05)', padding: '12px 30px', borderRadius: '15px', border: '1px solid #d4af37' }}>
          <span style={{ ...styles.goldLuster, fontSize: '12px' }}>STATUS: NOMINAL</span>
        </div>
      </header>

      {/* MAIN: TALL CONSOLE PANELS */}
      <div style={{ display: 'flex', gap: '50px', flex: '1', marginTop: '40px', zIndex: 5 }}>

        {/* SMOKECRAFT ATELIER */}
        <div style={styles.tallSlab}>
          <img src="https://images.unsplash.com/photo-1527030280862-64139fba04ca?w=1200" style={styles.imageLayer} alt="Cigar" />
          <div style={styles.overlay}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ ...styles.goldLuster, fontSize: '56px', marginBottom: '10px', fontStyle: 'italic' }}>Smokecraft</h2>
            <p style={{ fontSize: '12px', color: '#fff', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '40px', textTransform: 'uppercase' }}>
              Molecular Leaf Lab // Ritual Prep
            </p>
            <button style={{ 
              background: 'rgba(0,0,0,0.8)', 
              border: '1px solid #d4af37', 
              padding: '20px 50px', 
              color: '#d4af37', 
              fontWeight: '900', 
              letterSpacing: '0.5em',
              cursor: 'pointer',
              fontSize: '11px'
            }}>INITIATE CRAFT</button>
          </div>
        </div>

        {/* POURCRAFT CONSOLE */}
        <div style={styles.tallSlab}>
          <img src="https://images.unsplash.com/photo-1544027993-37dbfe43562a?w=1200" style={styles.imageLayer} alt="Pour" />
          <div style={styles.overlay}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ ...styles.silverLuster, fontSize: '56px', marginBottom: '10px', fontStyle: 'italic' }}>Pourcraft</h2>
            <p style={{ fontSize: '12px', color: '#fff', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '40px', textTransform: 'uppercase' }}>
              Vessel Geometry // Spirit Sync
            </p>
            <button style={{ 
              background: 'rgba(255,255,255,0.05)', 
              border: '1px solid rgba(255,255,255,0.3)', 
              padding: '20px 50px', 
              color: '#fff', 
              fontWeight: '900', 
              letterSpacing: '0.5em',
              cursor: 'pointer',
              fontSize: '11px'
            }}>ANALYZE POUR</button>
          </div>
        </div>

      </div>

      {/* FOOTER: INDUSTRIAL TELEMETRY */}
      <footer style={{ marginTop: 'auto', display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderTop: '1px solid rgba(255,255,255,0.1)', paddingTop: '30px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '20px' }}>
          <div style={{ 
            width: '18px', 
            height: '18px', 
            background: '#d4af37', 
            transform: 'rotate(45deg)', 
            boxShadow: '0 0 30px #d4af37' 
          }}></div>
          <span style={{ fontSize: '11px', letterSpacing: '0.6em', opacity: 0.4, textTransform: 'uppercase', color: '#fff' }}>
            Titan Engine // Live Telemetry
          </span>
        </div>
        <span style={{ ...styles.goldLuster, fontSize: '10px', opacity: 0.6 }}>Axiom Sovereign OS v3.0</span>
      </footer>
    </div>
  );
};

export default AxiomTerminal;