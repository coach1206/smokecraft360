import React from 'react';

// THE FINAL WIRING: This forces the terminal to not look like a website
const AxiomTerminal = () => {
  const styles = {
    shell: {
      height: '100vh',
      width: '100vw',
      backgroundColor: '#050505',
      background: 'radial-gradient(circle at 50% 0%, #1c1c1e 0%, #050505 100%)',
      color: '#fff',
      display: 'flex',
      flexDirection: 'column',
      padding: '40px',
      overflow: 'hidden',
      position: 'relative',
    },
    goldLuster: {
      background: 'linear-gradient(180deg, #fff9e6 0%, #d4af37 45%, #8a6d3b 100%)',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      fontWeight: '900',
      letterSpacing: '0.4em',
      filter: 'drop-shadow(0 0 15px rgba(212, 175, 55, 0.6))',
      textTransform: 'uppercase',
    },
    // THIS FORCES THE TALL LOOK: 70% of the screen height
    tallSlab: {
      flex: '1',
      height: '70vh', 
      borderRadius: '40px',
      position: 'relative',
      overflow: 'hidden',
      borderTop: '2px solid rgba(255, 255, 255, 0.3)', 
      borderLeft: '1px solid rgba(255, 255, 255, 0.1)',
      boxShadow: '0 50px 100px rgba(0,0,0,0.9)',
      background: '#121214',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      padding: '40px',
    },
    image: {
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      zIndex: 0,
      filter: 'brightness(1.3) contrast(1.1)', // MAKES IT BRIGHTER
    }
  };

  return (
    <div style={styles.shell}>
      <header style={{ zIndex: 10, display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.1)', paddingBottom: '30px' }}>
        <div>
          <h1 style={{ ...styles.goldLuster, fontSize: '32px' }}>Axiom 360</h1>
          <p style={{ fontSize: '10px', letterSpacing: '0.6em', opacity: 0.4, marginTop: '8px' }}>Sovereign Terminal // v3.0</p>
        </div>
        <div style={{ background: 'rgba(212, 175, 55, 0.05)', padding: '10px 30px', borderRadius: '15px', border: '1px solid #d4af37' }}>
          <span style={{ ...styles.goldLuster, fontSize: '12px' }}>VITALITY: 98%</span>
        </div>
      </header>

      <div style={{ display: 'flex', gap: '50px', flex: '1', marginTop: '40px', zIndex: 5 }}>
        {/* SMOKECRAFT 360 SLAB */}
        <div style={styles.tallSlab}>
          <img src="https://images.unsplash.com/photo-1527030280862-64139fba04ca?w=1200" style={styles.image} />
          <div style={{ position: 'absolute', inset: 0, background: 'linear-gradient(to top, #000 0%, transparent 60%)', zIndex: 1 }}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ ...styles.goldLuster, fontSize: '56px', marginBottom: '10px', fontStyle: 'italic' }}>Smokecraft</h2>
            <p style={{ fontSize: '14px', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '40px' }}>Molecular Leaf Lab</p>
            <button style={{ background: 'rgba(0,0,0,0.8)', border: '1px solid #d4af37', padding: '20px 50px', color: '#d4af37', fontWeight: '900', letterSpacing: '0.5em' }}>INITIATE RITUAL</button>
          </div>
        </div>

        {/* POURCRAFT 360 SLAB */}
        <div style={styles.tallSlab}>
          <img src="https://images.unsplash.com/photo-1544027993-37dbfe43562a?w=1200" style={styles.image} />
          <div style={{ position: 'absolute', inset: 0, background: 'linear-gradient(to top, #000 0%, transparent 60%)', zIndex: 1 }}></div>
          <div style={{ zIndex: 2 }}>
            <h2 style={{ color: '#fff', fontSize: '56px', marginBottom: '10px', fontStyle: 'italic' }}>Pourcraft</h2>
            <p style={{ fontSize: '14px', opacity: 0.6, letterSpacing: '0.4em', marginBottom: '40px' }}>Spirit Synchronization</p>
            <button style={{ background: 'rgba(255,255,255,0.05)', border: '1px solid rgba(255,255,255,0.2)', padding: '20px 50px', color: '#fff', fontWeight: '900', letterSpacing: '0.5em' }}>ANALYZE POUR</button>
          </div>
        </div>
      </div>

      <footer style={{ marginTop: 'auto', display: 'flex', justifyContent: 'space-between', borderTop: '1px solid rgba(255,255,255,0.1)', paddingTop: '30px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '20px' }}>
          <div style={{ width: '18px', height: '18px', background: '#d4af37', transform: 'rotate(45deg)', boxShadow: '0 0 30px #d4af37' }}></div>
          <span style={{ fontSize: '11px', letterSpacing: '0.6em', opacity: 0.4 }}>Titan Engine // Live Telemetry</span>
        </div>
        <span style={{ ...styles.goldLuster, fontSize: '10px', opacity: 0.6 }}>Axiom Sovereign OS</span>
      </footer>
    </div>
  );
};

export default AxiomTerminal;